[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20.19.0"
  NPM_CONFIG_REGISTRY = "https://registry.npmjs.org/"

# React/Vite SPA fallback: any client-side route must return index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
