Optional animation vendor folder.
The current project works with the Web Animations API.
A local GSAP build can be added later and exposed as window.gsap; motion-service.js will use it automatically.
